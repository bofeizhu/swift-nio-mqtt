name: Unit Test

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  test:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
